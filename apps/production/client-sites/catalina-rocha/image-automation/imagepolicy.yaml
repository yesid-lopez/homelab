apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: catalina-rocha
  namespace: lulo-demo-landings
spec:
  imageRepositoryRef:
    name: catalina-rocha
  policy:
    semver:
      range: ">=0.0.0"
  filterTags:
    pattern: '^v?[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
