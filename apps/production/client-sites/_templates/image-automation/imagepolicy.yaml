apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: __CLIENT__
  namespace: lulo-demo-landings
spec:
  imageRepositoryRef:
    name: __CLIENT__
  policy:
    semver:
      range: ">=0.0.0"
  filterTags:
    pattern: '^v?[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
