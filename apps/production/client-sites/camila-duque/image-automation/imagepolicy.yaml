apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: camila-duque
  namespace: lulo-demo-landings
spec:
  imageRepositoryRef:
    name: camila-duque
  policy:
    semver:
      range: ">=0.0.0"
  filterTags:
    pattern: '^v?[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
