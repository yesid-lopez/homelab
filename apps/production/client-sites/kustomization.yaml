apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared namespace + registry pull secret for all demo landings.
# Add new clients below after running:
#   scripts/new-client-site.sh <client>
resources:
  - namespace.yaml
  - sealed-registry-secret.yaml
  - configmap-demo-preview.yaml
  - daniela-rivas
  - jenny-psiquiatra
  - mariana-toro
  - jenny-florez
  - maria-fernanda-espana
  - katerin-osorio
  - veronica-mejia
  - catalina-rocha
