apiVersion: v1
kind: Service
metadata:
  name: thingiverse-mcp
  namespace: thingiverse-mcp
spec:
  selector:
    app: thingiverse-mcp
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8000
