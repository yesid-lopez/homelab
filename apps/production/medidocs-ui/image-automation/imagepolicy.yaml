apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: medidocs-ui
  namespace: medidocs-ui
spec:
  imageRepositoryRef:
    name: medidocs-ui
  policy:
    semver:
      range: ">=0.0.1"
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
