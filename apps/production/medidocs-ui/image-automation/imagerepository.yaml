apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: medidocs-ui
  namespace: medidocs-ui
spec:
  image: registry.yesidlopez.de/medidocs-ui
  interval: 1m
  secretRef:
    name: registry-secret
