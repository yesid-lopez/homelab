apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mi-carpeta-medica-api
  namespace: mi-carpeta-medica-prod
  annotations:
    cert-manager.io/cluster-issuer: luloai-issuer
    external-dns.alpha.kubernetes.io/target: homelab.luloai.com
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "20m"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [mi-carpeta-medica-api.luloai.com]
      secretName: mi-carpeta-medica-api-tls
  rules:
    - host: mi-carpeta-medica-api.luloai.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: {name: mi-carpeta-medica-api, port: {number: 80}}
