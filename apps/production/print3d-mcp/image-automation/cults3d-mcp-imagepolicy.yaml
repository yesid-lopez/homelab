apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: cults3d-mcp
  namespace: print3d-mcp
spec:
  imageRepositoryRef:
    name: cults3d-mcp
  policy:
    semver:
      range: ">=0.1.0"
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
