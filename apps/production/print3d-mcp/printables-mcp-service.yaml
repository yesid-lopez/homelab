apiVersion: v1
kind: Service
metadata:
  name: printables-mcp
  namespace: print3d-mcp
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.104"
spec:
  selector:
    app: printables-mcp
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8000
