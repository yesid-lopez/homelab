apiVersion: v1
kind: Service
metadata:
  name: thingiverse-mcp
  namespace: print3d-mcp
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.103"
spec:
  selector:
    app: thingiverse-mcp
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8000
