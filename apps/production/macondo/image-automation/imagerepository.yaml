apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: macondo
  namespace: macondo
spec:
  image: registry.yesidlopez.de/macondo
  interval: 1m
  secretRef:
    name: registry-secret 
