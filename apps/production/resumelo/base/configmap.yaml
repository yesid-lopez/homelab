apiVersion: v1
kind: ConfigMap
metadata:
  name: configmap
  namespace: resumelo
  labels:
    app: resumelo
data:
  # Langfuse configuration
  LANGFUSE_HOST: https://langfuse.yesidlopez.de
  LANGFUSE_TRACING_ENVIRONMENT: CHANGE_ME
