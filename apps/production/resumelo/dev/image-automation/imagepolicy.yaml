apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: resumelo-dev
  namespace: resumelo-dev
spec:
  imageRepositoryRef:
    name: resumelo
  policy:
    alphabetical:
      order: asc 
