apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: resumelo-ui-dev
  namespace: resumelo-ui-dev
spec:
  imageRepositoryRef:
    name: resumelo-ui
  policy:
    alphabetical:
      order: asc
