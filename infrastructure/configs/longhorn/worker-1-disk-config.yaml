---
apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: worker-1
  namespace: longhorn-system
spec:
  allowScheduling: true
  disks:
    # Existing default disk (NVMe) - keep original ID
    default-disk-9c19114f0501dc61:
      allowScheduling: true
      diskDriver: ""
      diskType: filesystem
      evictionRequested: false
      path: /var/lib/longhorn/
      storageReserved: 75464511897 # 75GB
      tags: ["nvme", "fast", "worker-1"]
    # Duplicate disk - disable first before removal
    worker-1-nvme:
      allowScheduling: false
      diskDriver: ""
      diskType: filesystem
      evictionRequested: false
      path: /var/lib/longhorn/
      storageReserved: 75464511897 # 75GB
      tags: ["nvme", "fast", "worker-1"]
    # New SSD disk
    worker-1-ssd:
      allowScheduling: true
      diskDriver: ""
      diskType: filesystem
      evictionRequested: false
      path: /mnt/longhorn-ssd
      storageReserved: 50000000000  # ~50GB reserved
      tags: ["ssd", "bulk-storage", "worker-1"]
  evictionRequested: false
  instanceManagerCPURequest: 0
  name: worker-1
  tags: []
