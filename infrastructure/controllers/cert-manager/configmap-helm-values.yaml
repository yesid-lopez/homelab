apiVersion: v1
kind: ConfigMap
metadata:
  name: cert-manager-helm-chart-value-overrides
  namespace: flux-system
  labels:
    reconcile.fluxcd.io/watch: "Enabled"
data:
  values.yaml: |-
    crds:
      enabled: true 
